import 'package:flutter/material.dart';

class MatchListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Match List")),
      body: Center(child: Text("Match List Page")),
    );
  }
}
