import 'package:flutter/material.dart';

class ScoreboardLivePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Live Scoreboard")),
      body: Center(child: Text("Live Scoreboard Placeholder")),
    );
  }
}
