import 'package:flutter/material.dart';

class InspectionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Inspection Page")),
      body: Center(child: Text("Admin - Inspection Page")),
    );
  }
}
